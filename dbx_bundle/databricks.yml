# This is a Databricks asset bundle definition for dbx_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dbx_bundle
  uuid: 1343bd69-e195-4708-9277-84e62cb3f3ab

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-4091704180100433.13.azuredatabricks.net
      root_path: /Users/tushar@hemaliriaangmail.onmicrosoft.com/dev/notebook_bundle      

  prod:
    mode: production
    workspace:
      host: https://adb-4091704180100433.13.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/tushar@hemaliriaangmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/tushar@hemaliriaangmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: tushar@hemaliriaangmail.onmicrosoft.com
        level: CAN_MANAGE
